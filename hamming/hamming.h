#if !defined(HAMMING_H)
#define HAMMING_H

#include <stdexcept>
#include <string>

namespace hamming {
using std::string;

int compute(string const &, string const &);

} // namespace hamming

#endif // HAMMING_H
